spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gen_0cd68058ca0d?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: myuser
    password: mypassword
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

logging:
  level:
    com.example.app: DEBUG
